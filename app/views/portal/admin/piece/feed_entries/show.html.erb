<p class="form">基本情報</p>

<table class="show">
  <%= piece_base_view @item, :container => :tr %>
</table>

<p class="form">詳細設定</p>

<table class="show">
  <tr>
    <th><%= @item.locale :category %></th>
    <td><%= safe{@item.category.title} %></td>
  </tr>
</table>

<%= creator_view @item %>
