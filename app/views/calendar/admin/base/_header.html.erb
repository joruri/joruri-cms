<div class="mainMenu">
<%= render :partial => 'layouts/admin/cms/menu', :locals => {:current => :content} %>
</div>

<div class="contentMenu">
<% current = defined?(current) && current ? current : :base %>
<%= link_to 'イベント一覧', calendar_events_path, :class => (current == :base ? "current" : "") %>
<%= link_to '設定', calendar_content_settings_path, :class => (current == :setting ? "current" : "") if @content.editable? %>
</div>
