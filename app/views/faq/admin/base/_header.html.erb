<div class="mainMenu">
<%= render :partial => 'layouts/admin/cms/menu', :locals => {:current => :content} %>
</div>

<div class="contentMenu">
<% current = defined?(current) && current ? current : :base %>
<%= link_to '記事一覧', faq_edit_docs_path, :class => (current == :base ? "current" : "") %>

<% if Core.user.has_auth?(:designer) %>
<%= link_to '分野設定', faq_categories_path(:parent => 0), :class => (current == :category ? "current" : "") %>
<%= link_to '再構築'  , faq_rebuild_path, :class => (current == :rebuild ? "current" : "") %>
<%= link_to '設定'    , faq_content_settings_path, :class => (current == :setting ? "current" : "") %>
<% end %>
</div>
